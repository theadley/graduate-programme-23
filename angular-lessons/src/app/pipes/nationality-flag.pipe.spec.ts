import { NationalityFlagPipe } from './nationality-flag.pipe';

describe('NationalityFlagPipe', () => {
  it('create an instance', () => {
    const pipe = new NationalityFlagPipe();
    expect(pipe).toBeTruthy();
  });
});
