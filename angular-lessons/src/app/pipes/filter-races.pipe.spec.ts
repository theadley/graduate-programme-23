import { FilterRacesPipe } from './filter-races.pipe';

describe('FilterRacesPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterRacesPipe();
    expect(pipe).toBeTruthy();
  });
});
